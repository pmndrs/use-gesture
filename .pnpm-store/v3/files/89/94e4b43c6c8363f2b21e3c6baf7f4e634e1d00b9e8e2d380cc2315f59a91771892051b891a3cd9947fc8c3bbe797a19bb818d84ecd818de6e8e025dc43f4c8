/**
 * Joins all given segments and converts using a forward slash (/) as a delimiter
 * @param segments A sequence of segments
 */
export declare function urlResolve(...segments: Array<string>): string;
