import { isInteger } from "../isInteger";

test("isInteger", () => {
  expect(isInteger(1)).toBe(true);
  expect(isInteger("1")).toBe(true);
  expect(isInteger(1.5)).toBe(false);
  expect(isInteger("1.5")).toBe(false);
});
