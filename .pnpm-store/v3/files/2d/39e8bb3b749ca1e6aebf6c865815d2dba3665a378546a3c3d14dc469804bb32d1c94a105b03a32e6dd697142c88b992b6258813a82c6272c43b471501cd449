import { Object3D, Camera } from 'three';
declare type Props = {
    all?: boolean;
    scene?: Object3D;
    camera?: Camera;
};
export declare function Preload({ all, scene, camera }: Props): null;
export {};
