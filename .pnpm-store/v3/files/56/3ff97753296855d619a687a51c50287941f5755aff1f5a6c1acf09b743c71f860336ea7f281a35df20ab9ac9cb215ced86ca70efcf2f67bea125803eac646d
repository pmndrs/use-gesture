/// <reference types="react" />
/**
 * Returns `true` if `event` has been fired within a React Portal element.
 */
export declare function isPortalEvent(event: React.SyntheticEvent<Element, Event>): boolean;
