---
title: Utilities
section: Reference
order: 4
---

import UseGesture from './code/rug'

# Utilities

&zwnj;<UseGesture pkg="react" /> uses a set of utility functions internally that are exposed for anybody's convenience.

### addV

Adds two vectors.

```jsx
import { addV } from '@use-gesture/react'

addV([10, 5], [5, 7]) // returns [15, 12]
```

### subV

Substracts two vectors.

```jsx
import { subV } from '@use-gesture/react'

subV([10, 5], [1, 2]) // returns [9, 3]
```

### rubberbandIfOutOfBounds

Calculates the rubberbanding effect from a given `position` value, two bounds `min`, `max` and an elasticity `constant`.

```ts
function rubberbandIfOutOfBounds(position: number, min: number, max: number, constant = 0.15): number
```
